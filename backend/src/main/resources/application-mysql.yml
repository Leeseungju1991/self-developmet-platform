spring:
  datasource:
    url: jdbc:mysql://localhost:3306/selfdev?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
